#include<iostream>
#include<bits/stdc++.h>
using namespace std;

void solve(int index,int *arr, vector<vector<int>> &ans )
{
    // base case 
    if(index >=  )
}

vector<vector<int>> permutations(int *arr)
{
    vector<vector<int>> ans;

    int index=0;

    solve(index,arr,ans);

}

int main()
{ 
    int arr[] = {1, 2, 3};
    permutations(arr);
return 0;
}